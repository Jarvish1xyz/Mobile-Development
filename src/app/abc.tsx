import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const abc = () => {
  return (
    <View>
      <Text>This will show everywhere</Text>
    </View>
  )
}

export default abc

const styles = StyleSheet.create({})